module.exports = {
  useProxy: false, // keep false unless proxies are provided

  proxies: [
    // Examples (DO NOT enable yet)
    // { host: "127.0.0.1", port: 8080 },
    // { host: "127.0.0.2", port: 8000 },
    // { host: "proxy.example.com", port: 3128, username: "user", password: "pass" }
  ],
};
